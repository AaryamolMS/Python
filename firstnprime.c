num=int(input("enter a number:"))
count=0
print("first",num,"prime numbers are:")
	while count<num:
	is_prime=true
	for i in range(2,int(start(n))+1)
		if(n%i)==0
			is_prime=false
				break;
		if is_prime_flag:
			print(n/end="")
			count=count+1
			n=n+1
